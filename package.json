{
  "name": "engness_type",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "prisma:generate": "cp -r ../Engness_Node/prisma/schema.prisma ./src/prisma && npx prisma generate --schema ./src/prisma/schema.prisma",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^5.7.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "zod": "^3.22.4"
  }
}
